# Agent Guide

- Start with @CLAUDE.md.
- Use @README.md for product context and @build.gradle for dependencies/tasks.
- Keep backend changes scoped to the controller/service/repository area you actually touch.
- After code changes, run the smallest useful verification from @.claude/rules/verification.md.
- Prefer referencing existing docs with `@filename` instead of copying them into new docs.
